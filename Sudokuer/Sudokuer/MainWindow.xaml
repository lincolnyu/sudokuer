<Window x:Class="Sudokuer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:Sudokuer.Views"
        Title="MainWindow" Height="550" Width="525"
        Name="TheWindow">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary 
                  Source="Resources/Base.xaml">
                </ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <DockPanel LastChildFill="True">
        <Grid DockPanel.Dock="Top" >
            <StackPanel Orientation="Horizontal" Margin="3">
                <TextBlock>Block Size:</TextBlock>
                <ComboBox Name="SubregionSizeComboBox"  ItemsSource="{Binding SubregionSizes}" Margin="5,0,0,0"
                  Text="{Binding SubregionSize, Converter={StaticResource IntToStringConverter}, Mode=TwoWay}"/>
                <Button Margin="10,0,0,0" Click="SolveClicked">Solve</Button>
            </StackPanel>
        </Grid>
        <views:SudokuPanel SubregionSize="{Binding SubregionSize, Converter={StaticResource IntToStringConverter}, Mode=TwoWay}"/>
    </DockPanel>
</Window>
